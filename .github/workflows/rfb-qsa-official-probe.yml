name: QSA Source Hierarchy Probe

on:
  pull_request:
    paths:
      - .github/workflows/rfb-qsa-official-probe.yml
      - backend/src/cli/probeRfbQsa.ts
      - backend/src/modules/public-data/brasilApiQsaFallback.ts
      - backend/src/modules/public-data/brasilApiQsaFallback.test.ts
      - backend/src/modules/public-data/strategicPublicDatasetConnector.ts
      - backend/src/services/strategicPublicIngestionService.ts
  workflow_dispatch:
    inputs:
      cnpj:
        description: CNPJ revisado para o probe de QSA
        required: true
        default: "17770708000124"
        type: string
      reference:
        description: Competência opcional da Receita (YYYY-MM); vazio tenta a mais recente
        required: false
        default: ""
        type: string
      max_resources:
        description: Máximo de partições Socios a verificar
        required: true
        default: "20"
        type: string

permissions:
  contents: read

concurrency:
  group: qsa-source-hierarchy-probe-${{ github.ref }}
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      TARGET_CNPJ: ${{ inputs.cnpj || '17770708000124' }}
      TARGET_REFERENCE: ${{ inputs.reference || '' }}
      MAX_RESOURCES: ${{ inputs.max_resources || '20' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Typecheck backend
        run: npm -C backend run typecheck
      - name: Test BrasilAPI QSA fallback privacy and provenance
        working-directory: backend
        run: npx tsx --test src/modules/public-data/brasilApiQsaFallback.test.ts
      - name: Probe QSA source hierarchy without persistence
        id: qsa_probe
        working-directory: backend
        shell: bash
        continue-on-error: true
        run: |
          mkdir -p "$RUNNER_TEMP/qsa-diag"
          ARGS=(
            --cnpj "$TARGET_CNPJ"
            --max-resources "$MAX_RESOURCES"
            --max-matched-rows 1000
            --require-match
          )
          if [ -n "$TARGET_REFERENCE" ]; then ARGS+=(--reference "$TARGET_REFERENCE"); fi
          set +e
          npx tsx src/cli/probeRfbQsa.ts "${ARGS[@]}" > "$RUNNER_TEMP/qsa-diag/qsa-probe.log" 2>&1
          EXIT_CODE=$?
          set -e
          cat "$RUNNER_TEMP/qsa-diag/qsa-probe.log"
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
      - name: Upload QSA probe diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qsa-source-hierarchy-diagnostics
          path: ${{ runner.temp }}/qsa-diag
          if-no-files-found: error
          retention-days: 7
      - name: Enforce QSA source hierarchy result
        if: always()
        shell: bash
        run: |
          test "${{ steps.qsa_probe.outputs.exit_code }}" = "0"
