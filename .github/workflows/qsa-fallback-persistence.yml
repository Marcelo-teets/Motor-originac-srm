name: QSA Fallback Persistence

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/qsa-fallback-persistence.yml
      - api/strategic-public-data-run.ts
      - backend/src/cli/qsaFallback.ts
      - backend/src/modules/public-data/brasilApiQsaFallback.ts
      - backend/src/services/qsaFallbackIngestionService.ts
      - db/migrations/102_qsa_fallback_governance.sql
      - db/migrations/103_qsa_fallback_idempotency.sql
      - vercel.json
  pull_request:
    paths:
      - .github/workflows/qsa-fallback-persistence.yml
      - api/strategic-public-data-run.ts
      - api/vercelTypes.ts
      - backend/src/cli/qsaFallback.ts
      - backend/src/modules/public-data/brasilApiQsaFallback.ts
      - backend/src/modules/public-data/brasilApiQsaFallback.test.ts
      - backend/src/services/qsaFallbackIngestionService.ts
      - backend/src/services/qsaFallbackIngestionService.test.ts
      - db/migrations/102_qsa_fallback_governance.sql
      - db/migrations/103_qsa_fallback_idempotency.sql
      - vercel.json

permissions:
  contents: read

concurrency:
  group: qsa-fallback-persistence-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Typecheck backend
        run: npm -C backend run typecheck
      - name: Compile shared strategic Vercel function
        run: npm -C backend exec -- tsc --noEmit --skipLibCheck --module NodeNext --moduleResolution NodeNext --target ES2022 --types node ../api/strategic-public-data-run.ts
      - name: Test QSA fallback normalization and activation
        working-directory: backend
        run: npx tsx --test src/modules/public-data/brasilApiQsaFallback.test.ts src/services/qsaFallbackIngestionService.test.ts
      - name: Guard signal, routing and source governance contract
        shell: bash
        run: |
          grep -F "if new.signal_type = 'ownership_structure_signal'" db/migrations/102_qsa_fallback_governance.sql
          grep -F "return null;" db/migrations/102_qsa_fallback_governance.sql
          grep -F "sourceAuthority', 'secondary_public_api'" db/migrations/102_qsa_fallback_governance.sql
          grep -F "outputsAvailable', v_outputs_available" db/migrations/103_qsa_fallback_idempotency.sql
          grep -F "where not exists" db/migrations/103_qsa_fallback_idempotency.sql
          grep -F "mode === 'qsa-fallback'" api/strategic-public-data-run.ts
          grep -F '"schedule": "30 10 8 * *"' vercel.json
          grep -F '"destination": "/api/strategic-public-data-run?mode=qsa-fallback"' vercel.json
          test ! -f api/qsa-fallback-run.ts

  ingest-production:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Validate ingestion secrets
        run: |
          test -n "$SUPABASE_URL" || (echo "SUPABASE_URL is missing" && exit 1)
          test -n "$SUPABASE_SERVICE_ROLE_KEY" || (echo "SUPABASE_SERVICE_ROLE_KEY is missing" && exit 1)
      - name: Bootstrap QSA fallback monitoring evidence
        working-directory: backend
        run: |
          npx tsx src/cli/qsaFallback.ts \
            --force \
            --max-companies 50 \
            --trigger backfill \
            --require-output \
            --require-zero-signals
