[project]
name = "motor-originacao-srm-backend"
version = "0.1.0"
description = "Consolidated backend for the Motor Originação SRM platform"
requires-python = ">=3.10"

dependencies = []

[tool.pytest.ini_options]
pythonpath = ["app", "."]
testpaths = ["tests"]
